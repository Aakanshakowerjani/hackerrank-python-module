def merge_the_tools(string, k):
    
    for i in range(0, len(string), k):
        s=string[i:i+k]
        l=[]
        for y in s:
            if y not in l:
                l.append(y)
        print(''.join(l))

        

